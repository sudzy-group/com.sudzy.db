{
  "name": "sudzy-db",
  "version": "0.0.0",
  "description": "sudzy-db",
  "license": "MIT",
  "repository": "",
  "author": "",
  "keywords": [
    ""
  ],
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "generate": "npm run build && cd generator && ./generate.sh && cd ..",
    "clean": "rimraf lib",
    "build": "npm run clean && echo Using TypeScript && tsc --version && tsc --pretty",
    "test": "npm run build && mocha --compilers ts:ts-node/register --recursive test/**/*-spec.ts",
    "cover": "npm run build && nyc report --reporter=html mocha --compilers ts:ts-node/register",
    "watch": "npm run build -- --watch",
    "watch:test": "npm run test -- --watch"
  },
  "dependencies": {
    "lodash": "^4.17.4",
    "pouchable": "^1.1.1",
    "pouchdb": "^6.1.2"
  },
  "devDependencies": {
    "@types/chai": "^3.0.0",
    "@types/mocha": "^2.0.0",
    "@types/node": "6.0.31",
    "acorn": "^4.0.11",
    "babel-plugin-istanbul": "^4.0.0",
    "chai": "^3.0.0",
    "markdown-table": "^1.1.0",
    "mocha": "^3.0.0",
    "mocha-typescript": "^1.0.22",
    "nyc": "^10.1.2",
    "rimraf": "^2.0.0",
    "ts-node": "^1.0.0",
    "tspoon": "^1.0.328",
    "typescript": "^2.0.0"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "nyc": {
    "include": [
      "src/**/*.ts"
    ],
    "exclude": [
      "lib",
      "src/index.ts"
    ],
    "extension": [
      ".ts"
    ],
    "require": [
      "ts-node/register"
    ],
    "reporter": [],
    "all": true
  }
}
